package com.dietetica.lembas.orders.model;

/** Fulfillment modes supported by an order. The MVP only allows pickup at the branch. */
public enum FulfillmentType {
    /** Customer picks up the order at the branch. */
    PICKUP
}
