package com.dietetica.lembas.suppliers.model;

/** Preview classification for a supplier price update row. */
public enum PriceUpdateBatchItemStatus {
    CREATE,
    UPDATE,
    UNCHANGED,
    REVIEW,
    EXCLUDED,
    ERROR
}
