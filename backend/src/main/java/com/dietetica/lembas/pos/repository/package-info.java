/**
 * Repository interfaces for the POS module.
 *
 * <p>POS does not own tables; it delegates persistence to {@code catalog} and
 * {@code inventory}. This package is intentionally empty and exists only to
 * preserve the project's pkg-by-feature structure.</p>
 */
package com.dietetica.lembas.pos.repository;
